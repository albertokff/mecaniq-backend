version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mecaniq_postgres
    environment:
      POSTGRES_DB: mecaniq_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: